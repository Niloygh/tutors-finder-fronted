import Profile from '@/component/Profile';
import React from 'react';

export const metadata = {
  title: "Tutor Finder | Profile Page",
};

const ProfilePage = () => {
    return (
        <div>
            <Profile />
        </div>
    );
};

export default ProfilePage;